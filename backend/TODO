gameEventsManager.class.ts:100

delete optionId for cellFunction (EXCEPT #3) if really not needed

----------------------- improvements
add timeout before game start and after it was open

revive all active and open games in revive service (the same timeout as the next)

add timeout on disconnect while playing game

implement users and profile (user) api

implement chat

----------------------- possible issue sources
non-sparse indexes in users collection (e.g. null is unique)